const valores = [1, 2, 3, 4]

console.log(valores.length) // tamanho
valores.push(6, 1, 1) // adiciona osse elementos ao array

console.log(valores.pop) // tiara o ultimo elemento do array
delete valores[0]